export const APP_NAME = 'AI Vehicle Health Monitor';

export const STORAGE_KEYS = {
  AUTH_TOKEN: 'auth_token',
  USER: 'user_data',
};

export const DEFAULTS = {
  PAGE_SIZE: 20,
};

export default { APP_NAME, STORAGE_KEYS, DEFAULTS };
